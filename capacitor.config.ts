import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hr.apps.pokedex',
  appName: 'Pokedex',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
